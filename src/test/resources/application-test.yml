observability:
  sla:
    live-detection:
      enabled: false          # prevents scheduler beans from loading in slice tests;
                              # tests needing live detection set it via @TestPropertySource
  security:
    basic:
      username: test
      password: test

logging:
  level:
    com.company.observability: DEBUG
